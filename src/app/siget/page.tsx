import { Dashboard } from "@/components/(base)/dashboard";

export default function DashboardPage() {
  return (
    <div className="flex flex-1 flex-col min-h-0 bg-muted dark:bg-zinc-950">
      <Dashboard />
    </div>
  );
}
